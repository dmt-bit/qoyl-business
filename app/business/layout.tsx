// /business/* reuses the hair seller shell (session gate + sidebar) so the
// performance page lives at the URL sellers were told about while sharing
// one layout with /fake-hair-brand/*.
export { default } from "@/app/fake-hair-brand/layout";
